---
description: Run smoke test after code changes to verify build, tests, and live API
---

Run the CrabClaw smoke test to verify all changes work end-to-end.

// turbo-all

1. Run the smoke test script:
```bash
cd /Users/jakevin/code/crabclaw && ./scripts/smoke-test.sh
```

2. If the smoke test fails, investigate the failure and fix it before proceeding.
